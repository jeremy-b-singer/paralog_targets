\copy (select avg(score) as avg_score, td.tax_id as original_tax_id, td.organism as orig_organism, md.pref_name, md.chembl_id,journal, year, volume, issue, first_page, last_page,pubmed_id, doi, title, authors from hmmer_statistics h  join target_dictionary td   on h.target = td.chembl_id  join drug_mechanism dm  ON dm.tid = td.tid  join molecule_dictionary md  ON dm.molregno = md.molregno JOIN compound_records cr ON md.molregno = cr.molregno join docs on cr.doc_id = docs.doc_id WHERE h.tax_id = 5807 and score >= 423.4 group by td.tax_id, td.organism, md.pref_name, md.chembl_id ,journal, year, volume, issue, first_page, last_page, pubmed_id, doi, title, authors order by md.pref_name, year, volume, issue) to 'C:/Users/Jeremy-satellite/Documents/RBIF120/paralog_targets/supplements/targets/C_parvum_hmmer_drugs_annotated.txt' CSV HEADER delimiter '		'
